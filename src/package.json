{
  "name": "src",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "start": "ls -a && node dist/index.js",
    "build": "esbuild './cron/**/*.ts' --bundle --packages=external --platform=node --target=node18 --outdir=dist --outbase=cron --minify --sourcemap --format=esm --resolve-extensions=.ts,.js",
    "dev": "vite-node -w cron/index.ts",
    "test": "vitest"
  },
  "license": "MIT",
  "description": "",
  "devDependencies": {
    "@types/jsonwebtoken": "^9.0.8",
    "@types/node": "^22.12.0",
    "globals": "^15.11.0",
    "vitest": "^3.0.2"
  },
  "dependencies": {
    "esbuild": "^0.23.1",
    "vite-node": "^2.1.2"
  }
}
